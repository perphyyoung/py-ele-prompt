# 方案要求/修改计划

- 给出实现方案或计划时, 必须考虑到方案的可维护性, 可扩展性, 性能等因素, 不能只考虑当前需求;
- 方案中不要出现"或", 如果有多个选项, 通过数字序号列出, 给出简要说明
- 禁止使用 powershell 替换, 可以使用 js 替换
- 当我未输入 **"修改"** 或 **"实施"** 时, 不允许立即改动代码, 只允许输出准备修改的思路, 代码块非必要的话不需要展示
