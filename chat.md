# 开发对话记录

## 常用

根据 chat.md 的第 12 行以及对应的对话内容, 更新 readme.md 和 change.md, 并总结后输出到对话中, 用于 git commit 信息, 需要输出详细版本和简洁版本
正常了, 删除日志的生成语句, 保留日志系统
调试解决, 写入 debug.log, 不是 控制台
全面分析当前项目, 有没有什么常见的隐患, 给出优化方案
排查上步所有可能的问题, 给出修改方案
检查现有事件绑定中, 有哪些是没有在 html 中出现的, 也没有 动态生成; 给出统计结果
更新 js文件树.md

## 20260321-2

快捷键管理
generateUniqueTimestamp 删除
main.js add-prompt 提取日期函数, substr
逐个检查 renderer/utils 目录, 到 utils 目录
创建提示词时, toast 不是中文; 参考 DialogService, 实现统一管理的 toast
renderer\services\DialogService.js 设置相同的提示风格信息
现在 两个回收站的操作逻辑是否一致, 变量命名是否一致, 还能再优化吗?
图像详情界面, 原本无提示词关联, 添加关联后, 图像主界面没有刷新, 分析原因并修复
提示词详情界面, 添加图像后, toast 提示 添加成功
提示词主界面和图像主界面, 是否也可以使用配置驱动模式, 与当前实现有何优劣
批量操作界面, 是否也可以使用配置驱动模式, 与当前实现有何优劣
现在 两个回收站的整体设计, 可以怎样重构, 结合 refator skills 分析
图像详情界面, 解除提示词关联后, 切换到提示词主界面, 提示词主界面没有更新, 分析原因
新建提示词 界面, 如果新上传图像已经存在于数据库中, 则提示用户是否确认覆盖; 跳转到 当前图像的图像详情界面
图像引用计数 定期清理
设计图像引用计数方案, 参考 database.js 中的建表语句, 结合 sqlite-optimization skills 分析

- 简化 ImageUploadService 只保留核心上传功能
- 删除未使用的临时文件相关代码
- 删除命名不清的 deleteImageFile API
- 修复 CSP 内联事件处理器安全错误
- 修复图像详情界面新增上传后不渲染问题
- 修复上传成功提示信息出现 object 的问题
- 取消上传预览中的"设为首张"功能
- 实施事件委托优化减少事件监听器数量
- 优化进度提示逻辑，小批量不显示进度
- 采用 Safe File Dialogs 替代 `<input type="file">` ，优化内存使用
- 重构图像上传为延迟保存策略：选择 → 预览 → 确认保存
- 提取可复用的上传策略类（DelaySaveStrategy、DirectSaveStrategy）
- 支持多图上传，统一各入口上传逻辑
- 配置 CSP 安全策略和路径验证，增强安全性
- 添加批量上传进度反馈
- 修复 Windows 路径验证，支持任意盘符
- 统一导航系统：合并 KeyboardNavigator 到 ListNavigator，删除 clickLeft/clickRight 导航
- 修复导航按钮状态：修复从非第一张打开详情界面时左边按钮禁用状态错误
- 修复全屏 ESC 问题：修复图像全屏后按 ESC 直接返回主界面而非详情界面的问题
- 统计页面改造：改为悬浮面板样式，左右分栏布局，修复数值计算错误
提示词详情界面, 提示词标签区域不能添加标签, 而 图像详情界面的可以添加, 比较差异, 给出修改方案
标签筛选的头部, 即标签筛选区域收起时, 设置显示的高度只比 内部的标签 上下高度各 5px, 充分利用空间; 左侧的 标签筛选 文字区域, 恢复只有一个单元, 删除现在两个单元的设计, 标签筛选和清除筛选公用一个单元; 当点击了标签后, 标签筛选 的文字变为 清除筛选, 并支持清除筛选的功能; 结合代码, 说下理解, 给出方案
新建提示词, 如果没有填写提示词内容, 直接点击 完成按钮, 则提示用户 提示词内容不能为空
新建提示词页面, 点击或拖拽上传图像 区域点击后, 弹出文件选择窗口, 选中图像, 文件选择窗口短暂关闭后又打开了, 选中的图像也没有加载到页面上; 重复绑定问题? 修复; 可以参考 提示词详情界面 上传图像的设计

## 20260321-1

- 修复双向禁止二级跳转功能, 设置二级跳转时按钮禁用样式
- 修复 addImagePrompts/addPromptImages 函数 FOREIGN KEY constraint 错误处理
- 修复 ImageDetailManager.unlinkFromPrompt 使用 p.id 而非 p.promptId 的 bug
- 数据模型.md 补充 promptRefs 完整字段说明 和 prompt.images 完整字段说明
- 修复事件绑定中 HTML 不存在的 ID
- 统一提示词详情界面 ID 命名为 promptDetailXXX 格式, 统一图像详情界面 ID 命名为 imageDetailXXX 格式
- 实施优化（EventBus.once、错误处理、操作锁、缓存同步）
- 分析 HTML 两侧详情界面 ID 命名一致性
- 修复提示词详情修改图像关联后图像面板不刷新
- EventBus添加一次性订阅方法
- 统一renderer层错误处理使用logError写入文件
- savePromptField添加操作锁防止重复提交
- savePromptField完善缓存同步
- ImageDetailManager解除关联时完善缓存同步和面板刷新
- 全面分析项目隐患并实施优化
二级跳转 的禁止正常了, 但样式有点问题, 二级页面是 图像详情时, 需要在 hover 时出现禁止的图标; 参考 提示词详情界面的禁止样式 或 app copy 中的设置
现在双向禁止二级跳转的逻辑是什么?
现在双向禁止二级跳转的逻辑太复杂了, 只需要记录当前界面是从主界面还是其他详情界面跳转过来就行; 如果是从其他详情界面, 则禁止继续跳转; 与当前实现的区别和利弊
设置禁止二级跳转, 即从图像详情界面跳转到提示词详情界面后, 不能再通过点击图像左上的眼睛图标, 跳转到图像详情界面; app copy 中有实现, 可以参考, 样式要与重构前一致
loadCardBackgrounds 添加 await? -> loadCardBackgrounds 目的是预加载图片，不应阻塞 UI 渲染。外层无需 await。
通过 图像详情界面 的 编辑提示词按钮打开 提示词详情界面, 设置返回到图像详情界面, 而不是现在的图像主界面
通过 提示词详情界面 改变 提示词关联的图像, 图像主界面没有更新, 点击刷新后界面更新, 分析原因并修复
提示词详情界面 哪有 修改图像关联 按钮? 修改图像关联是通过 图像右上的 x 取消关联的, 通过下方的两个区域添加关联; 仔细对照 html 结构, 检查问题并修复
提示词详情界面, 通过 从图像管理选择 按钮, 选中的图像, 没有出现在参考图像区域, 分析原因并修复
继续检查两个主界面的id重命名后, js 是否已同步修改
html 中, 提示词和图像两个详情界面的对应id, 命名是否一致, 包括命名顺序等常见问题, 给出统计报告; 如果不一致, 给出修改方案
参考 提示词详情界面 ID 统一方案, 给出 图像详情界面的 ID 统一方案
ImagePanelManager.subscribeToEvents 现在监听了哪些事件, 监听到后会怎么操作, 是否合理, 是否遗漏
全面检查现有实现中, 需要更新 图像或提示词 的 updated_at 时, 不是通过 updatePrompt/updateImage 的方式, 给出统计报告
现在 提示词详情界面 和 图像详情界面 的更新逻辑是否一致, 还能再优化吗?
图像详情界面的更新方式, 也参考当前 提示词详情界面的更新方式, 统一放入一个函数集中管理, 给出修改方案
图像详情界面, 左侧面板可以更改与提示词的关联, 也需要更新图像的 updated_at 字段, 给出修改方案
现在 提示词详情 界面, 图像操作的更新逻辑是什么? 是否都会更新 提示词的 updated_at
提示词详情添加图像关联后(其他字段没变), 提示词主界面没有更新, 已经是按最近更新排序了, 分析原因并修复
图像主界面的 上传图像, 和 新建提示词 界面的 上传图像, 以及 提示词详情界面的 两个上传图像, 现在分别是怎么实现的? 有几种类型, 可以怎样使用统一方案优化
提示词详情界面, 点击 左侧的 点击或拖拽上传图像 区域, 弹出选择对话框, 点击图像后, 再次出现选择对话框, 选择的图像没有出现在参考图像区域, 给出修复方案; 参考重构前

## 20260320-1

- 统一新建提示词的 images 参数格式为对象数组
- 修复上传图像创建提示词后图像详情面板不显示提示词信息的问题
当前事件驱动模式, 是否同时保持了与缓存的一致
反过来呢, 如果 图像方面改变, 提示词界面可能也需要刷新, 能否复用 事件驱动
新建提示词界面, 添加带图像的提示词成功后, 切换到 图像主界面, 没有出现图像, 刷新后出现,分析原因
新建提示词界面, 添加带图像的提示词成功后, 切换到 图像主界面, 没有出现图像, 刷新后出现,分析原因, 给出修复方案
- 对话框消息支持 \n 换行
- renderImagePreviews 优化：缓存命中跳过数据库查询，未命中按 ID 批量获取
- database.js 图像查询：新增 getImagesByIds、getImagesCore、getPromptRefsForImages 消除 N+1 问题
- database.js 提示词查询：重构 getFavoritePrompts、getDeletedPrompts 使用批量查询
- DialogService.showConfirmDialogByConfig 改为静态方法，直接调用无需中间层
- DialogConfig 按功能分类重组：删除类、移动/恢复类、清空/重置类、其他
- 单按钮对话框：DATA_RESET 添加 singleButton 属性，确定按钮居中显示
- 确认对话框支持 \n 换行，使用 innerHTML 替代 textContent
重新组织 DialogConfig 的顺序, 按照功能分类, 给出建议
统一改为 showConfirmDialogByConfig, 给出修改方案
设置里的 清空数据 改为 重命名当前数据目录并创建新的空数据目录
仔细检查还有哪个的 showConfirmDialog 调用方式不正确
BatchOperationsManager.js 存在需要同时移除 image 和 prompt 缓存的场景吗? 可以怎样优化 -> 传递类型信息, 根据类型判断是否全部执行
TagRegistry.js 不需要 renderView 吗? -> 标签操作不需要刷新主界面
更改代码时, 需要同步更改注释
loadData 总是 从数据库加载数据到缓存 吗? renderView 总是 从缓存获取数据 吗? 两个方法都分别加上 ToCache 和 FromCache 是否更好?
全部改为通过 CacheManager 管理, 给出修改方案 -> 统一使用 CacheManager 管理缓存
现有项目中, 有哪些地方用到了缓存, 是否都是 LRU 缓存, 给出统计结果
创建 公用 utils 目录, 用于存放 公共的工具函数
当前项目的 图像id 与 提示词id 的生成规则, 是否一致
DialogService 用于显示确认对话框, 确认对话框的标题和消息内容, 可以自定义
使用配置驱动模式优化回收站代码
get-prompt-trash 仍然同时获取提示词和图像

## 20260319-2

- 标签筛选区多选键改为Ctrl/Cmd（行业通用方案）
- 将"清除筛选"按钮移到"标签筛选"标题右侧
- 特殊标签徽章颜色改为与普通标签一致
- 删除标签时的确认对话框绑定Enter和Esc键
- 收起状态下预留"清除筛选"按钮位置避免跳变
- 暂不考虑提示词详情界面的字段; 图像详情界面, 上次分析缺少了 收藏(isFav), 标签, 以及 提示词关联(取消关联) 的分析; 给出这 6个 字段 的现有保存情况的分析; 结合 refactor skills 给出优化方案
- 提示词详情界面, 修改 多图状态后, 主界面的标签筛选区的 计数没有更新, 点击 多图 标签后, 数值变更, 分析原因; 其他特殊标签是否会及时更新; 图像主界面的特殊标签呢
- 导航到首张或末张时, 以及直接进入首张或末张时, 设置对应的导航按钮变灰
- 图像详情界面, 导航的顺序没有考虑到主界面的排序, 分析原因; 与 提示词导航 的区别
- 生成 js文件树.md, 用于记录项目中用到的 js 文件, 以文件树的格式记录, 每行 文件名右侧 简述该文件的核心功能
- 根据 检查本次重构是否完成预期功能
- 上次分析没考虑 PromptDetailManager; 详情视图方法与 ImageDetailManager 紧密耦合, 结合 PromptDetailManager 和 refactor skills 分析, 怎样优化
- 基于以上分析, 给出详细的 appjs二次重构方案.md, 并保留 重构进度 章节, 用于追踪重构进度
- app.js 还是有点大, 和预期差异明显; 基于整个项目, 再次分析现在的 app.js 可以怎样重构, 参考 refactor skills 分析
- ImageViewerManager 如果只是和全屏相关, 修改为 ImageFullscreenManager ; 其他按照优先级实施
- 基于整个项目, 现在的 app.js 可以怎样重构, 参考 refactor skills 分析 -> 基于以上分析, 给出详细的 appjs重构方案.md
- 当前重构向后兼容, 保留了原有的方法别名，确保现有调用代码无需修改; 改为无需向后兼容, 直接删除原有的方法别名, 涉及的函数有哪些?
- TagHtmlGenerator.js 设置只支持 Set类型, 删除 Array 类型的处理逻辑
- TagFilterSummaryRenderer 改为 TagFilterHeader; 其他按照你的设计, 开始实施重命名
- ListRenderer 改为 PanelRenderer, CardBuilder 改为 PanelItemRenderer; 还有哪些准备改名的项目, 给出命名对应表
- ListRenderer 和 CardBuilder 现在分别的功能是什么? 怎样命名可以更符合实际功能? PanelItemBuilder?
- 两个主面板的 列表视图和紧凑视图, 也可以统一实现吧; 按照 CardBuilder 类的实现, 重构这两个视图的代码; ListRenderer 和 CardBuilder 的关系是?
- 根据以上分析, 生成 统一实现重构分析.md; 并根据优先级 逐步实施重构
- 当前项目的 提示词管理 和 图像管理, 有哪些本该 统一实现 但却没有 统一实现 的方面, 结合 refactor skills 分析

## 20260319-1

- 修复提示词标题未修改却显示已保存的问题
- 移除保存状态中的图标，改为纯文字显示（绿色已保存、红色保存失败）
- 实现关闭模态框时自动保存所有未保存的变更
- 实现导航切换时自动保存当前变更
- 图像详情页同步实现相同保存逻辑
- 普通标签 drag 到卡片, 添加标签
- 统计当前提示词主界面 标签筛选区的功能, 用于同步到图像, 给出修改方案
- 提示词和图像的 id 命名风格统一; promptEdit -> promptDetail
- 图像标签筛选右上角的 收起标签 收起和展开状态, 不需要文字显示, 参考 重构前
- 根据优化后的 `d:\develop\comfy-common\py-ele-prompt\database.js` , 修改项目中用到数据库的调用, 给出统计
- 根据 sqlite-optimization skills, 分析 database.js, 生成 数据库优化方案.md
- `d:\develop\comfy-common\py-ele-prompt\.trae\skills` 根据这几个 skills, 分析当前项目的缓存设计, 给出优化方案, 输出 缓存优化方案.md

## 20260317-6

- 将上述分析提取为 重构分析-提示词与图像面板.md
- 当前项目的提示词主界面和图像主界面, 提取为对应的类来实现是否更好, 难度如何? 可以通过 readme.md 和 参考文档 目录了解项目功能; 结合 refactor skills 分析

## 20260317-5

- 添加 skills: electron-best-practices, sqlite-optimization, 删除 py-ele-prompt-dev
- 更新文档结构, 参考文档目录下新增 特定功能.md, 通用功能.md, 设计规范.md, 数据模型.md
- 提取 功能需求.md, 提示词方面与图像方面相同的部分, 在通用功能部分说明; 在 readme 和 需求文档中只引用; 说下你的理解, 以及修改方案

## 20260317-4

- 实现提示词与图像安全评级双向联动更新
- 设计事件驱动架构，创建 SafeRatingService 集中管理联动逻辑
- 移除数据库层联动代码，遵循单一职责原则
- 修复提示词编辑界面 toggle 状态不同步问题
- 修复 refreshCurrentPromptEdit 选择器错误（使用#promptSafeToggle 替代.safe-status-toggle）
- 提示词和图像现在还有联动更新 toggle 吗
- 实现 textarea 自动调整高度功能，使用业界通用方案（重置 height 为 auto 后设置为 scrollHeight）
- 修复 autoResizeTextarea 在初始化时 scrollHeight 为 0 的问题，使用 requestAnimationFrame 确保 DOM 更新后计算
- 修复 bindFieldEvents 中 this 指向错误，将 autoResize 逻辑从 SaveManager 迁移到 PromptManager
- 简化 autoResize 实现，直接在 updatePromptEditView 中绑定 input 事件，避免复杂的配置传递
- 添加防重复绑定标记（autoResizeBound），避免每次调用 updatePromptEditView 时重复绑定事件监听器
- 为图像详情界面的备注字段（imageDetailNote）实现相同的 autoResize 逻辑
- 将 bindEditPromptInputListeners 中的功能, 完全迁移到 SaveManager 中, 然后删除; 如果代价太高, 给出优化方案
- updateEditPromptView 和 updateImageDetailView 改为一致的命名风格, 给出建议

## 20260317-3

- 修复图像详情界面关闭时 saveAllImageDetailFields is not a function 错误, 扩展 SaveManager.saveAll() 方法支持图像详情上下文
- 迁移 JavaScript 到 ES Module 支持 ( import / export ), 修改所有的 require 为 import; preload.js 重命名为 preload.cjs, 并在 main.js 中引用
- 分离 Constants 类到独立文件
- 添加项目版本信息配置 (package.json, .nvmrc, readme.md)
- 记录 Node.js/Electron/JavaScript 版本要求
- 将相关版本信息, 写入相应的配置文件, 列出要修改的文件和内容
- 当前项目使用的js版本是多少, 有不符合 es6 的代码吗
- 迁移到 ES Module, 支持 import/export, IDE 更好的代码提示和重构支持
- 常量类 从 app.js 中分离出是否更好
- 将上步修改涉及的 字符串字面量 添加到 常量类, 给出修改计划
- 恢复文本字段的保存提醒, 比如 提示词标题 的 已保存 和 保存失败, 给出方案

## 20260317-2

- 重构保存和更新逻辑，提取 SaveManager 类统一管理
- 实现字段级变化追踪，避免不必要的数据库更新
- 修复提示词卡片打开后未操作却改变排序的问题
- 当前的保存和更新逻辑太分散了吧, 结合 refactor skills, 给出优化方案
- 当前 提示词编辑界面的保存和更新逻辑梳理
- 结合 refactor skills, 给出提示词编辑界面和图像详情界面通用的保存和更新的优化方案
- 将上述保存和更新逻辑, 重构为一个类是否更好? 同时考虑 图像详情界面的保存和更新
- 删除特殊标签的特殊样式，统一使用通用样式
- 清理 JavaScript 中无用的特殊标签类名引用
- 统一普通标签样式为3种：筛选标签、展示标签、可编辑标签
- 删除旧标签样式定义（卡片标签、列表标签等）
- 修改 JavaScript 中标签类名引用，使用新的统一样式类名
- 根据当前项目要求, 生成项目专属的 skills 文件夹 py-ele-prompt-dev
- 添加 refactor 文件夹, 用于存放代码重构 skills

## 20260317-1

- 提示词界面图像显示标签, 样式参考图像卡片
- 提示词编辑界面的图像, 所有的标签都显示在图像的底部, 叠加在图像上, 最多显示3行, 再多需要点击展开卡片, 给出方案
- TagManager 通过构造函数注入的 getTagsWithGroup 方法来处理 tag group
- 图像详情界面, tags的保存机制与其他元素有什么不同 -> TagManager
- 统一标签自动补全逻辑, 提取通用自动补全方法
- 提示词编辑界面, 输入标签, 候选词好像不完整, 和 图像详情界面的不同, 分析差异 -> setupTagAutocomplete
- 添加标签失败时,不清空输入(可能是输入错误,想更改),是否可以简化, 有何利弊
- 实现双向禁止二级跳转功能（提示词编辑界面↔图像详情页）, 在二级界面的跳转按钮上添加置灰样式和禁用状态, Hover 时显示 "禁止二级跳转" 提示
- 在新打开的二级界面上禁用跳转, 并在 禁用的按钮上 hover 显示 "禁止二级跳转", 给出实现方案
- 如何禁止二级跳转, 即 从提示词编辑界面1, 进入图像查看界面2后, 禁止再通过界面2的按钮进入新的界面1
- 删除提示词和图像的 toggle 联动, 给出修改方案
- :star: 现有的涉及数据展示的功能, 哪些不是从数据库直接读取, 而是依赖缓存或类似的计数, 给出分析结果
- 新规范要写入项目规则

## 20260316-3

- 提取通用ID比较方法`isSameId()`和`findPromptById()`，统一处理字符串/数字ID类型不匹配
- 将所有数据对象ID比较（提示词、图像、分组）强制使用新方法
- 将DOM元素ID比较也强制使用`isSameId()`方法
- :star: 强制统一使用转为字符串比较来比较 dom id, 提取通用方法, 注意复用
- Constants 常量类
- svg 是否可以提取变量实现复用

## 20260316-2

- 统一三个界面导航按钮样式，提取通用 .nav-btn-floating 类
- 提示词编辑界面导航按钮从底部移到两侧悬浮，删除底部操作栏
- 为 .nav-btn-floating 添加 focus 样式，移除点击后的视觉变化
- ListNavigator 构造函数添加初始按钮状态更新，修复单张图像时禁用状态不生效问题
- 只有一张图像时, next和last不是禁用状态, 修复方案是否复杂 -> 所有 ListNavigator 实例在创建时立即更新导航按钮状态
- 全屏的导航按钮点击后, 再导航, 上次点击的按钮会有样式变化, 分析原因 -> 浏览器默认的 focus 样式 行为, 改为为导航按钮添加 CSS，移除视觉变化
- 统一导航按钮禁用状态处理，使用 CSS 类替代 disabled 属性
- 将导航按钮状态管理集中到 ListNavigator 类，通过 navButtons 参数传入按钮元素
- 统一为 ListNavigator 实现, 说下修改方案
- 其他的导航是否有类似问题? 作用原理有何不同
- 全屏, 点击 最后一张按钮 后, 再点击导航, 导航失效, 分析原因 -> 移除按钮的 disabled 属性，改用 CSS 控制样式，保持事件监听
- 还有什么全局快捷键? -> 只剩 1 个全局 keydown 监听
- 移除提示词编辑界面标题聚焦，避免导航干扰
- 修复提示词编辑界面输入时导航键意外触发问题，添加输入框焦点检查
- 统一图像和提示词列表多选的输入框焦点检查，复用 PromptManager.isInputFocused()
- 删除所有 Escape 快捷键处理
- 提示词编辑界面, 修改提示词标题时, 导航会切换到下一张, 和 图像详情界面的文件名编辑, 导航逻辑有什么不同
- 进入提示词编辑界面后, 不要聚焦到提示词标题, 说下理解
- 统一图像和提示词编辑的导航实现模式, 封装了通用的列表导航功能 ListNavigator, 确保两个界面的导航行为一致
- 创建 EditableTagList 组件封装标签列表渲染和事件处理
- 提取通用方法 bindTagContainerEvents 实现图像和提示词标签的事件委托, 确保两个界面的标签删除、toast 提示都正常
- 实施图像标签的事件委托
- 图像标签管理有代码异味吗? -> 代码异味-图像标签管理.md
- 命名体现出变量/函数/类的作用和类型, 避免太泛的通用名称

## 20260316-1

- 统一 addTagWithViolationCheck 和 checkSingleSelectViolation 方法，将 TagManager 和 PromptManager 中的重复实现合并为 TagManager 的静态方法
- 设为首图改为立即保存到数据库
- 删除提示词界面的脏数据标记机制（所有字段都有独立自动保存）
- 添加 getNormalTags 方法统一过滤所有特殊标签
- 图像详情界面, 删除标签没有提示, 而提示词编辑界面有, 分析原因
- 提示词编辑界面, 导航到含有 违单 的数据时, 再导航会弹出 选择保存哪个 的界面, 分析并给出方案
- 使用 powershell 替换文件, 导致文件编码问题, 回退到托盘重启功能添加的时候
- 在 方案要求.md 中添加: 给出实现方案时, 必须考虑到方案的可维护性, 可扩展性, 性能等因素, 不能只考虑当前需求; 方案中不要出现"或", 如果有多个选项, 通过数字序号列出,给出简要说明
- 托盘添加重启功能

## 20260315-1

- 重构文件名和备注保存逻辑，提取通用字段保存管理器，通过配置对象管理字段行为，减少代码重复
- 图像详情界面备注编辑框实现同样的防抖保存逻辑，添加状态显示
- 像详情界面文件名编辑框添加防抖保存模式，记录原始值，保存时检查变更，标题右侧显示"已保存"或"保存失败"状态
- 文件名和备注的保存逻辑, 是否可以重构
- 图像详情界面的备注, 实现同样逻辑
- 图像详情界面的文件名编辑框, 设为防抖保存模式, 记录当前值, 保存时检查; 文件名标题右侧给出 "已保存" 或 "保存失败"状态显示; 给出设计方案, 分析优劣
- 重构提示词相关函数，提取 mapRowToPrompt 辅助函数统一对象映射逻辑
- 重构图像相关函数，提取 mapRowToImage 辅助函数统一对象映射逻辑
- 检查数据库传入传出的命名规范，修复 is_safe 字段在前端接口中使用驼峰命名 isSafe
- 上述数据能否重构
- 当前数据库的传入传出, 有哪些不符合命名规范
- 将项目规则中的命名规范, 提取为 命名规范.md

## 20260314-2

- 修复图像批量收藏后标签区域不更新问题
- 修复图像批量删除后标签区域不更新问题
- 统一提示词和图像的数据加载架构（loadXxx() → render() → [renderTagFilters(), renderList()]）
- 优化提示词批量删除避免重复调用 loadPrompts()

## 20260314-1

- 重构 hover 预览代码，提取通用 bindHoverPreview 方法，减少重复代码
- 简化图像加载逻辑，直接加载原图替代渐进式加载（缩略图→原图）
- 将 tooltip 移到 body 级别，解决面板切换导致不显示的问题
- 统一 hover 位置计算逻辑，mousemove 做边界检查
- 实现图像卡片 hover 预览功能，复用 HoverTooltipManager 的渐进式图像加载
- hover 同时显示提示词内容和图像 -> 提示词内容立即加载, 图像渐进式加载
- 提示词列表点击行为改为与图像列表一致：普通点击打开编辑，只有 Ctrl/Shift 点击才处理选择
- 提示词方面, 有一个选中后, 点击列表项会切换选中, 而图像方面, 有一个选中后, 点击列表项还是会打开图像详情; 比较下两个点击设计的思路, 给出利弊分析

## 20260313-2

- 分离出 特殊指令, 手动触发生效
- 图像方面, 批量添加的标签, 没有出现在主界面的标签区域里, 参考提示词方面实现
- 提示词紧凑模式, 添加首图, 实现参考图像方面, 给出修改计划
- 提示词列表多选功能
- 提示词标签和图像标签更新改为增量方式（ updatePrompt ）, 只删除需要移除的标签关联, 只添加新增的标签, 减少不必要的数据库操作
- 计划在提示词列表添加多选功能; 先总结当前的图像列表与提示词列表在多选功能不一致的地方, 并给出修改提示词列表的详细方案
- 文件名与标签同行: 标签靠右排列，文件名自适应
- 三种视图切换：卡片视图、列表视图、紧凑视图
- 批量收藏左边前面添加一个反选
- 除了取消选择, 其他3个按钮操作后, 不要退出选择模式; 比较下与当前实现的优劣
- 批量操作后保留选择模式
  - 批量收藏、批量添加标签、批量删除后不退出选择
  - 批量删除时从选择中移除已删除的图像
- 根据选中图像状态显示批量收藏或批量取消收藏
- renderImageGrid 是在图像列表中使用吗? 有没有renderImageList, 是否可以区分? 比较利弊
- 标签筛选与选择状态同步
  - 点击标签筛选时清除图像选择状态
  - 修复展开/收起状态下标签点击事件不一致问题
  - 清除筛选时同步清除选择
- 事件处理统一
  - 将图像列表视图详情打开逻辑合并到 bindListViewCheckboxEvents
  - 避免 Shift+点击范围选择时意外打开详情
- 图像列表多选功能, 新增紧凑模式
  - 批量删除, 收藏, 添加标签
  - 添加列表视图复选框，支持单选、多选、全选
  - 支持 Ctrl+点击、Shift+范围选择
  - 键盘快捷键：Ctrl+A 全选、Delete 删除、Esc 取消选择

## 20260313-1

- 未引用图像通过图像详情页面点击添加提示词, 打开新建提示词页面, 设定传入图像为预填充图像, 不支持删除, 该页面点击取消时保留该图像, 如果同时添加了其他图像, 点击取消时则删除其他图像
- 导出孤儿文件功能 -> 导出并删除
- 图像详情界面, 文件名和备注添加即时保存模式

## 20260312-2

- 数据目录不存在时自动创建
- 提示词编辑界面改为即时保存模式
- 界面简化: 图像上传界面只保留图像和提示词内容; 分离提示词编辑和提示词新建, 新建界面只保留提示词内容和图像
- 统一收藏按钮样式为28px圆形，添加悬停显示效果
- 创建design-system.md设计规范文档，包含色彩、间距、圆角系统
- 简化 project-rule.md和需求文档.md，删除重复设计规范内容
- 在README.md添加项目文档职责说明
- 按markdownlint规范格式化design-system.md
- 统一图像和提示词的单选组标签冲突处理逻辑
- 实现图像标签拖拽到图像卡片功能, 提示词标签拖拽到提示词卡片功能
- 分析图像和提示词标签管理的代码复用程度

## 20260312-1

- 特殊标签上下居中问题
- 优化 specialTags 多处重复定义，提取为 PromptManager 静态常量和静态方法
- 统一图像系统添加标签行为，与提示词系统一致，输入特殊标签仅提醒不清空输入框
- 在标签管理卡片首位组的标题栏显示"首位组"标识
- 修复提示词标签管理中未分组标签不显示的问题：修改标签分组逻辑，只有当 groupId 存在且对应组存在于 groups 数组中时才放入分组，否则放入未分组，解决标签引用不存在组时丢失的问题；同步修复图像标签管理的相同问题
- 统一图像和提示词标签筛选器行为：修改图像标签筛选器 renderImageTagFilters ，改为只遍历实际使用的标签（计数>0），与提示词标签筛选器 renderTagFilters 保持一致，解决图像方面计数为0的标签也显示在主界面的问题
- 为图像和提示词标签组卡片添加排序数值显示，支持右键菜单固定到首位功能，统一右键菜单组件实现，修复主界面标签筛选区按sortOrder排序显示的问题

- 统一图像标签筛选与提示词标签筛选界面设计
  - 图像标签筛选区域布局改为与提示词一致（左-右布局，收起按钮在右侧）
  - 标签名称简化（"未引用"→"未引"、"多引用"→"多引"、"使用数量"→"数量"）
  - "清除筛选"按钮移到顶部右侧按钮组，收起时仍显示
  - 修复单选组标签在标题栏的选中逻辑（Shift+点击也限制单选）
  - 修复点击"逆序"时排序下拉框不更新的问题
  - 图像标签管理界面顶部按钮布局与提示词一致（等宽排列）
  - 新建图像标签对话框支持选择标签组，与提示词界面一致
  - 修复图像标签管理界面点击"逆序"时排序框不更新的问题

## 20260311

- 标签组功能实现
  - 统一图像标签筛选与提示词标签筛选界面设计，包括布局、交互逻辑和功能一致性
  - 特殊标签虚拟卡片 - 将特殊标签（收藏、多图、无图、敏感）放入独立卡片，显示在标签组卡片区域最前面，使用淡紫色渐变背景区分
  - 未分组标签虚拟卡片 - 将未分组标签放入独立卡片，显示在特殊标签卡片之后，使用灰色渐变背景区分，支持编辑、删除、拖拽
  - 工具栏位置调整 - 将搜索框、新建标签、排序下拉、逆序按钮4个按钮移到最上方
  - 移除旧布局 - 删除原来的特殊标签区域和未分组标签列表区域，统一使用卡片式布局
  - 单向拖拽功能 - 仅支持从未分组卡片拖入标签组卡片（加入组），不支持从标签组拖入未分组，移出组需通过编辑按钮
- 提示词管理界面标签筛选区域重构 - 普通标签在右，收起功能，逆序功能
- IPC 通道命名优化 - 命名更清晰，区分"更新组属性"和"分配标签到组"两种不同操作
  - update-prompt-tag-group → update-prompt-tag-group-attrs（更新标签组属性）
  - update-prompt-tag-belong-group → assign-prompt-tag-to-belong-group（分配标签到所属组）
- 提示词标签单选组功能完善 - 数据层面实现单选
- 提示词标签管理功能完善
  - 标签筛选单选/多选逻辑 - 默认单选，Shift多选，单选组内互斥
  - 新建标签入口 - 标签管理界面添加"+ 新建标签"按钮
  - 标签名重复处理 - 检测已存在标签，提示是否覆盖并显示当前所属组
  - 居中显示 - 标签组子项和标签项列表居中排列
- 实现标签组管理系统 - 包括数据库表结构（prompt_tag_groups/image_tag_groups）、标签管理界面（创建/编辑/删除组、标签分配组）、标签筛选按组展示

2026-03-10 17:48:00：图像与提示词解除关联时, 同时改变更新时间
2026-03-10 17:45:00：图像标签管理和提示词标签管理界面的特殊标签区域添加 justify-content: center 样式，使特殊标签从默认左对齐改为居中显示
2026-03-10 17:42:00：提取通用卡片基础样式(.card)和元素样式(.card__bg/.card__overlay/.card__header/.card__footer/.card__btn)，重构图像回收站/提示词回收站/图像管理/提示词管理四个卡片组件
2026-03-10 18:20：优化项目文档结构，将 project-rule.md 精简为 AI 开发约束，在 需求文档.md 中补充完整的开发规范和流程，建立双向引用确保需求一致性。
2026-03-10 18:00: 调整提示词编辑页面布局，将提示词标签移到提示词翻译下方；为必需字段（提示词标题、提示词内容、上传图像的选择图像）添加 required 类和红色星号标记，使用紫色标签特殊显示。
2026-03-10 17:51: 修复提示词管理卡片首图与编辑界面图像不一致的问题，原因是已删除的图像仍被用作首图背景，通过在 loadCardBackgrounds 中过滤已删除图像解决。
2026-03-10：修改提示词回收站为卡片布局，与图像回收站风格一致；提示词内容不截断，完整显示；删除提示词确认对话框文字改为"已删除的 提示词 会进入回收站，可以从回收站恢复"，与实际软删除操作一致。
2026-03-10 17:50: 统一卡片收藏按钮交互，图像卡片和提示词卡片的收藏按钮均设置为只在hover时显示，删除按钮固定在右上角。
2026-03-10 17:35: 修改图像和缩略图存储目录结构，按年月分类存储（格式：202603），原图保存到 images/202603/ 目录，缩略图保存到 thumbnails/202603/ 目录；修复图像上传时更新时间设置，确保新上传图像的更新时间与上传时间一致。
2026-03-10 17:25: 添加图像按文件大小排序功能。
2026-03-10 17:05: 图像卡片改为背景图+遮罩层实现，添加底部区域显示标签和动态内容（根据排序规则显示更新/创建时间、文件名或尺寸），将未引用标记移至右下角。
2026-03-10 16:45: 提示词卡片底部改为动态显示，根据排序规则显示更新时间、创建时间或标题。
2026-03-10 16:40: 图像详情界面信息区域改为列表式显示，移除标签后的冒号，添加CSS样式美化。
2026-03-10 16:35: 修复所有使用 CURRENT_TIMESTAMP 的数据库更新操作，统一使用 new Date().toISOString() 存储 UTC 时间。
2026-03-10 16:30: 从图像查询函数中删除 md5 和 thumbnailMD5 返回，仅保留 getImageByMD5 用于后台重复校验。
2026-03-10 16:20: 图像详情界面将 MD5 显示改为更新时间显示，修复 getImageById 返回 fileSize 字段。
2026-03-10 16:05: 删除数据库迁移脚本，建表语句已包含所有字段，新建数据库无需迁移。
2026-03-10 16:00: 修改一键清空功能，不再删除原图像文件，仅清空数据库记录，并更新设置界面和确认对话框的说明文字。
2026-03-10 15:50: 添加图像文件大小记录功能，包括数据库字段 file_size、后端获取文件大小、前端格式化显示。
2026-03-10 15:40: 在 images 表添加 gen_params 字段（JSON格式）用于存储图像生成参数，在 prompts 表添加 content_translate 字段用于存储翻译内容。
2026-03-10 15:30: 删除数据库表中的 extra1, extra2, extra_json 字段，优化数据库结构。

2026-03-10 15:20: 将项目许可证从 MIT 切换为 GPL-3.0，更新 LICENSE 文件和相关说明。
2026-03-10 15:10: 调整图像排序选项顺序，将"最近更新"和"最早更新"设为前两项。
2026-03-10 14:55: 统一模态框尺寸，设置界面和统计界面使用 modal-content-medium，重命名 fullscreen 为 large，清理 HTML 中的组合类。
2026-03-09 22:56: 优化图像列表项和提示词列表项的CSS样式，将共用样式提取为基础类，简化代码结构。
2026-03-09 22:40: 优化图像列表项和提示词列表项代码，提取共用的标签生成函数 generateTagsHtml 和备注生成函数 generateNoteHtml，减少重复代码。
2026-03-09 22:20: 为提示词列表项添加提示词内容和备注显示，标签显示为单独单元，图像计数显示在最左侧。
2026-03-09 21:50: 为图像列表项添加备注和提示词内容显示，标签显示为单独单元，备注和标签显示在同一行。
2026-03-09 18:45: 实现图像和提示词安全评级双向联动，修改任一方安全评级时自动同步更新关联的另一方。
2026-03-09 18:35: 为提示词标签管理界面和图像标签管理界面添加安全评级特殊标签，根据 view_mode 控制显示。
2026-03-09 18:10: 在设置界面添加 view_mode 选项（safe/nsfw），用于控制内容显示模式，safe 模式只显示安全内容。
2026-03-09 18:00: 为图像查看界面和提示词编辑界面添加 is_safe 功能，用于控制安全评级。

2026-03-09 16:24: 渐进式重构图标系统，将常用图标（copy、delete、favorite）提取为变量 this.ICONS，统一使用 14x14 尺寸。
2026-03-09 12:45: 为提示词列表项和图像列表项添加 hover 显示预览功能，提示词列表项 hover 显示首图，图像列表项 hover 显示提示词内容。
2026-03-09 12:25: 为提示词管理界面添加卡片与列表视图切换功能，列表视图支持显示首图缩略图。
2026-03-09 12:15: 为图像管理界面添加卡片与列表视图切换功能，列表视图显示缩略图、文件名、标签和操作按钮。
2026-03-09 12:05: 为提示词管理界面、图像管理界面的标签筛选区域添加排序功能，支持按数量和名称排序。
2026-03-09 11:55: 为提示词标签管理、图像标签管理界面添加排序功能，支持按使用数量和名称排序。
2026-03-09 11:35: 为所有搜索框添加清空按钮，包括提示词管理、图像管理、选择图像、提示词标签管理、图像标签管理界面。
2026-03-09 11:20: 为提示词管理界面、图像管理界面、选择图像界面的搜索功能添加备注支持。
2026-03-09 11:04: 为所有 textarea 输入框添加自动调整高度功能, 比如: 提示词内容, 图像备注等。
2026-03-09 10:33: 为选择图像界面添加独立的排序功能，并统一控件宽度样式。
